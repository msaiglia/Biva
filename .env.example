# Copia questo file in .env (mai committare .env con valori reali)

# Connection string Neon pooled (per l'app in runtime)
DATABASE_URL="postgresql://user:password@ep-xxxxx-pooler.region.aws.neon.tech/biva?sslmode=require"

# Connection string Neon diretta (per le migrazioni Prisma)
DIRECT_URL="postgresql://user:password@ep-xxxxx.region.aws.neon.tech/biva?sslmode=require"
